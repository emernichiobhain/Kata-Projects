{
  "name": "prime-factors-kata",
  "version": "1.0.0",
  "description": "Prime Numbers Kata",
  "main": "index.js",
  "scripts": {
    "pretest": "npm run lint",
    "test": "npx mocha --reporter spec --exit \"./prime-numbers/**/*.spec.js\" || exit 0",
    "start": "node server.js",
    "lint": "eslint *.js **/*.js || exit 0",
    "prewatch": "npm run test",
    "watch": "onchange './prime-numbers/**/*.js' -v -- npm run test"
  },
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^5.6.0",
    "eslint-plugin-mocha": "^5.2.0",
    "mocha": "^5.2.0",
    "onchange": "^4.1.0"
  }
}
